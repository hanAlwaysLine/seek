<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace和定义的Mapper接口对应，并实现其中的方法 -->
<mapper namespace="cn.manage.mapper.StudentMapper">
    <resultMap type="cn.manage.bean.Student" id="studentResultMap">
        <id property="studentId" column="student_id"/>
        <result property="studentName" column="student_name"/>
        <result property="studentAge" column="student_age"/>
        <result property="sex" column="sex"/>
        <result property="telephone" column="telephone"/>
        <result property="school" column="school"/>
        <result property="major" column="major"/>
        <result property="email" column="email"/>
        <result property="qualification" column="qualification"/>
        <result property="company" column="company"/>
        <result property="pay" column="pay"/>
    </resultMap>
    <!-- id和mapper接口中的方法名对应，resultMap使用mybatis.xml中的别名 -->
    <select id="getStudent" resultMap="studentResultMap">
       select * from STUDENT
   </select>
    <insert id="saveStudent" parameterType="java.util.Map">
       insert into student(STUDENT_ID,STUDENT_NAME,student_age,sex,telephone,school,major,email,qualification,company,pay) values(#{studentId},#{studentName},#{studentAge},#{sex},#{telephone},#{school},#{major},#{email},#{qualification},#{company},#{pay});
   </insert>

</mapper>