<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace和定义的Mapper接口对应，并实现其中的方法 -->
<mapper namespace="cn.manage.mapper.TeacherMapper">
    <resultMap type="cn.manage.bean.Teacher" id="TeacherResultMap">
        <id property="teacherId" column="teacher_id"/>
        <result property="teacherName" column="teacher_name"/>
        <result property="position" column="POSITION"/>
        <result property="profile" column="PROFILE"/>
        <result property="teacherImage" column="TEACHER_IMAGE"/>
    </resultMap>
    <!-- id和mapper接口中的方法名对应，resultMap使用mybatis.xml中的别名 -->
    <select id="getTeacher" resultMap="TeacherResultMap">
       select * from teacher
   </select>
    <insert id="saveTeacher" parameterType="java.util.Map">
       insert into teacher(TEACHER_ID,TEACHER_NAME,POSITION,PROFILE,TEACHER_IMAGE) values(#{teacherId},#{teacherName},#{position},#{profile},#{teacherImage});
   </insert>

</mapper>